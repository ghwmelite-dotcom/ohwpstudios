---
import { SITE_CONFIG } from '@/config';
import Logo from '@/components/Logo.astro';
---

<header class="header">
  <nav class="nav container">
    <a href="/" class="logo-link">
      <Logo size="md" showText={true} />
    </a>

    <ul class="nav-links">
      <li class="nav-dropdown">
        <a href="/#services" class="dropdown-trigger">
          Services
          <span class="nav-badge hot">🔥 Hot</span>
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </a>
        <div class="dropdown-menu">
          <div class="dropdown-menu-content">
            <div class="dropdown-grid">
              <div class="dropdown-col">
                <h4>Services</h4>
                <a href="/services/custom-web-application-development">Web App Development</a>
                <a href="/services/saas-application-development">SaaS Development</a>
                <a href="/services/api-development-services">API Development</a>
              </div>
              <div class="dropdown-col">
                <h4>Technologies</h4>
                <a href="/technologies/fullstack-development">Fullstack</a>
                <a href="/technologies/react-development">React</a>
                <a href="/technologies/nextjs-development">Next.js</a>
                <a href="/technologies/nodejs-development">Node.js</a>
                <a href="/technologies/python-development">Python</a>
              </div>
              <div class="dropdown-col">
                <h4>Solutions</h4>
                <a href="/solutions/ai-powered-application-development">AI Development</a>
                <a href="/solutions/startup-mvp-development">Startup MVP</a>
              </div>
              <div class="dropdown-col">
                <h4>Industries</h4>
                <a href="/industries/fintech-software-development">Fintech</a>
                <a href="/industries/healthcare-software-development">Healthcare</a>
                <a href="/industries/education-software-development">Education</a>
              </div>
            </div>
          </div>
        </div>
      </li>
      <li class="nav-dropdown">
        <a href="#" class="dropdown-trigger">
          Explore
          <span class="nav-badge explore">🚀 Discover</span>
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </a>
        <div class="dropdown-menu">
          <div class="dropdown-menu-content">
            <div class="dropdown-grid">
              <div class="dropdown-col">
                <h4>Company</h4>
                <a href="/#portfolio">
                  Portfolio
                  <span class="nav-badge premium" style="margin-left: 0.5rem;">✨</span>
                </a>
                <a href="/case-studies">
                  Case Studies
                  <span class="nav-badge trending" style="margin-left: 0.5rem;">🎯</span>
                </a>
                <a href="/careers">
                  Careers
                  <span class="nav-badge hiring" style="margin-left: 0.5rem;">💼</span>
                </a>
              </div>
              <div class="dropdown-col">
                <h4>Free Tools</h4>
                <a href="/estimate-project">
                  Scope My Project
                  <span class="nav-badge premium" style="margin-left: 0.5rem;">✨</span>
                </a>
                <a href="/website-analyzer">
                  Website Grader
                  <span class="nav-badge hot" style="margin-left: 0.5rem;">🚀 Free</span>
                </a>
                <a href="/quiz">
                  Project Quiz
                  <span class="nav-badge hot" style="margin-left: 0.5rem;">🔥</span>
                </a>
              </div>
              <div class="dropdown-col">
                <h4>Insights</h4>
                <a href="/blog">
                  Blog
                  <span id="blog-post-counter-dropdown" class="nav-counter pulse" style="margin-left: 0.5rem;">...</span>
                </a>
                <a href="/sitemap-pages" class="view-all">View All Pages →</a>
              </div>
            </div>
          </div>
          </div>
        </div>
      </li>
      <li>
        <a href="/open-build" class="nav-link-with-badge">
          Open Build
          <span class="nav-badge hot">🔴 Live</span>
        </a>
      </li>
    </ul>

    <div class="nav-actions">
      <button class="theme-toggle" id="theme-toggle" aria-label="Toggle theme">
        <svg class="sun-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="5"/>
          <line x1="12" y1="1" x2="12" y2="3"/>
          <line x1="12" y1="21" x2="12" y2="23"/>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
          <line x1="1" y1="12" x2="3" y2="12"/>
          <line x1="21" y1="12" x2="23" y2="12"/>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
        </svg>
        <svg class="moon-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="display: none;">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
      </button>

      <a href="/estimate-project" class="btn btn-primary header-cta">
        <svg class="cta-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 2L2 7l10 5 10-5-10-5z"/>
          <path d="M2 17l10 5 10-5"/>
          <path d="M2 12l10 5 10-5"/>
        </svg>
        <span class="cta-text">Scope My Project</span>
        <span class="cta-text-short">Scope</span>
      </a>

      <button class="mobile-menu-toggle" aria-label="Toggle menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: var(--glass-bg);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--glass-border);
    box-shadow: var(--shadow-sm);
  }

  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: var(--header-height);
    gap: 2rem;
  }

  .logo-link {
    text-decoration: none;
    display: flex;
    align-items: center;
    transition: transform 0.3s ease;
  }

  .logo-link:hover {
    transform: translateY(-2px);
  }

  #header-logo-img:hover {
    transform: scale(1.05);
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-links a {
    color: var(--color-text);
    font-weight: 500;
    font-size: 0.95rem;
    transition: color var(--transition-fast);
    position: relative;
  }

  [data-theme="dark"] .nav-links a {
    color: rgba(255, 255, 255, 0.9);
  }

  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--gradient-primary);
    transition: width var(--transition-base);
  }

  .nav-links a:hover {
    color: var(--color-primary);
  }

  [data-theme="dark"] .nav-links a:hover {
    color: #818cf8;
  }

  .nav-links a:hover::after {
    width: 100%;
  }

  /* Dropdown Navigation */
  .nav-dropdown {
    position: relative;
  }

  .dropdown-trigger {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .dropdown-trigger svg {
    transition: transform var(--transition-fast);
  }

  .nav-dropdown:hover .dropdown-trigger svg {
    transform: rotate(180deg);
  }

  .dropdown-menu {
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    padding-top: 1.5rem; /* Padding to bridge the gap */
    opacity: 0;
    visibility: hidden;
    transition: all var(--transition-base);
    pointer-events: none;
  }

  .dropdown-menu::before {
    /* Invisible bridge to prevent gap */
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1.5rem;
  }

  .dropdown-menu-content {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    min-width: 600px;
    padding: var(--spacing-lg);
    transform: translateY(-10px);
    transition: transform var(--transition-base);
  }

  .nav-dropdown:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    pointer-events: all;
  }

  .nav-dropdown:hover .dropdown-menu-content {
    transform: translateY(0);
  }

  .dropdown-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--spacing-lg);
  }

  .dropdown-col h4 {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text-muted);
    margin-bottom: var(--spacing-sm);
    font-weight: 600;
  }

  .dropdown-col a {
    display: block;
    padding: 0.5rem 0;
    color: var(--color-text);
    font-size: 0.9rem;
    transition: all var(--transition-fast);
  }

  .dropdown-col a::after {
    display: none;
  }

  .dropdown-col a:hover {
    color: var(--color-primary);
    padding-left: 0.5rem;
  }

  .dropdown-col a.view-all {
    color: var(--color-primary);
    font-weight: 600;
    margin-top: var(--spacing-sm);
  }

  /* Creative Navigation Badges */
  .nav-link-with-badge {
    position: relative;
    display: inline-flex !important;
    align-items: center;
    gap: 0.5rem;
  }

  .nav-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.2rem 0.55rem;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: 12px;
    white-space: nowrap;
    transition: all var(--transition-fast);
  }

  /* Hot Badge - Pulsing Red/Orange */
  .nav-badge.hot {
    background: linear-gradient(135deg, #ff6b6b, #ee5a24);
    color: white;
    animation: badgePulse 2s ease-in-out infinite;
    box-shadow: 0 0 10px rgba(255, 107, 107, 0.5);
  }

  /* Trending Badge - Electric Blue */
  .nav-badge.trending {
    background: linear-gradient(135deg, #4facfe, #00f2fe);
    color: white;
    animation: badgeGlow 2s ease-in-out infinite alternate;
    box-shadow: 0 0 10px rgba(79, 172, 254, 0.5);
  }

  /* Premium Badge - Gold Shimmer */
  .nav-badge.premium {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 50%, #f093fb 100%);
    background-size: 200% 200%;
    color: white;
    animation: badgeShimmer 3s linear infinite;
    box-shadow: 0 0 10px rgba(240, 147, 251, 0.5);
  }

  /* Hiring Badge - Green Glow */
  .nav-badge.hiring {
    background: linear-gradient(135deg, #11998e, #38ef7d);
    color: white;
    animation: badgeBounce 2s ease-in-out infinite;
    box-shadow: 0 0 10px rgba(56, 239, 125, 0.5);
  }

  /* Counter Badge - Modern Notification Style */
  .nav-counter {
    position: absolute;
    top: -8px;
    right: -12px;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 22px;
    height: 22px;
    padding: 0 6px;
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    font-size: 0.7rem;
    font-weight: 700;
    border-radius: 12px;
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
    border: 2px solid var(--color-bg);
    transition: all var(--transition-fast);
  }

  .nav-counter.pulse {
    animation: counterPulse 2s ease-in-out infinite;
  }

  /* Badge Animations */
  @keyframes badgePulse {
    0%, 100% {
      transform: scale(1);
      box-shadow: 0 0 10px rgba(255, 107, 107, 0.5);
    }
    50% {
      transform: scale(1.05);
      box-shadow: 0 0 20px rgba(255, 107, 107, 0.8);
    }
  }

  @keyframes badgeGlow {
    from {
      box-shadow: 0 0 10px rgba(79, 172, 254, 0.5);
    }
    to {
      box-shadow: 0 0 20px rgba(79, 172, 254, 0.8), 0 0 30px rgba(79, 172, 254, 0.4);
    }
  }

  @keyframes badgeShimmer {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  @keyframes badgeBounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-3px);
    }
  }

  @keyframes counterPulse {
    0%, 100% {
      transform: scale(1);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
    }
    50% {
      transform: scale(1.15);
      box-shadow: 0 4px 20px rgba(102, 126, 234, 0.6), 0 0 30px rgba(102, 126, 234, 0.3);
    }
  }

  /* Hover Effects */
  .nav-link-with-badge:hover .nav-badge {
    transform: translateY(-2px);
  }

  .nav-link-with-badge:hover .nav-counter {
    transform: scale(1.1) rotate(5deg);
  }

  .nav-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  /* Header CTA Button - Expert Responsive Design */
  .header-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    min-height: 44px;
    box-sizing: border-box;
    padding: 0.6rem 1.25rem;
    font-size: 0.9rem;
    font-weight: 600;
    white-space: nowrap;
    border-radius: var(--radius-lg);
    background: var(--gradient-primary);
    color: white;
    text-decoration: none;
    transition: all var(--transition-base);
    box-shadow: 0 2px 8px rgba(99, 102, 241, 0.25);
    position: relative;
    overflow: hidden;
  }

  .header-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
  }

  .header-cta:hover::before {
    left: 100%;
  }

  .header-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(99, 102, 241, 0.35);
  }

  .header-cta:active {
    transform: translateY(0);
  }

  .cta-icon {
    flex-shrink: 0;
    transition: transform var(--transition-base);
  }

  .header-cta:hover .cta-icon {
    transform: scale(1.1) rotate(5deg);
  }

  .cta-text-short {
    display: none;
  }

  .mobile-menu-toggle {
    display: none;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 4px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
    min-width: 44px;
    min-height: 44px;
  }

  .mobile-menu-toggle span {
    width: 24px;
    height: 2px;
    background: var(--color-text);
    border-radius: 2px;
    transition: all var(--transition-base);
  }

  /* Tablet Breakpoint - Optimize for medium screens */
  @media (max-width: 1024px) {
    .header-cta {
      padding: 0.55rem 1rem;
      font-size: 0.85rem;
      gap: 0.4rem;
    }

    .cta-icon {
      width: 16px;
      height: 16px;
    }
  }

  /* Small Tablet - Show shorter button text */
  @media (max-width: 900px) {
    .cta-text {
      display: none;
    }

    .cta-text-short {
      display: inline;
    }

    .header-cta {
      padding: 0.55rem 0.9rem;
      font-size: 0.85rem;
    }
  }

  @media (max-width: 768px) {
    .header {
      padding: 0 var(--spacing-md);
    }

    .nav {
      height: 70px;
    }

    .logo {
      font-size: 1.25rem;
    }

    #header-logo-img {
      height: 38px !important;
      max-width: 140px !important;
    }

    /* Mobile badge adjustments */
    .nav-badge {
      font-size: 12px;
      padding: 0.15rem 0.45rem;
    }

    .nav-counter {
      top: -6px;
      right: -10px;
      min-width: 20px;
      height: 20px;
      font-size: 0.65rem;
    }

    /* Compact CTA for mobile */
    .header-cta {
      padding: 0.5rem 0.85rem;
      font-size: 0.9rem;
      gap: 0.35rem;
    }

    .cta-icon {
      width: 15px;
      height: 15px;
    }

    .nav-actions {
      gap: 0.6rem;
    }

    .nav-links {
      position: absolute;
      top: 70px;
      left: 0;
      right: 0;
      flex-direction: column;
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      padding: 2rem var(--spacing-md);
      gap: 1.5rem;
      border-bottom: 1px solid var(--glass-border);
      transform: translateY(-100%);
      opacity: 0;
      pointer-events: none;
      transition: all var(--transition-base);
      max-height: calc(100vh - 70px);
      overflow-y: auto;
    }

    .nav-links.active {
      transform: translateY(0);
      opacity: 1;
      pointer-events: all;
    }

    /* Mobile dropdown styles */
    .dropdown-menu {
      position: static;
      transform: none;
      padding-top: 0.5rem;
      min-width: auto;
      width: 100%;
      display: none;
    }

    .dropdown-menu-content {
      min-width: auto;
      padding: var(--spacing-md);
      transform: none;
    }

    .nav-dropdown.active .dropdown-menu {
      display: block;
      opacity: 1;
      visibility: visible;
    }

    .nav-dropdown.active .dropdown-menu-content {
      transform: none;
    }

    .dropdown-trigger {
      justify-content: space-between;
      width: 100%;
    }

    .dropdown-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-md);
    }

    .mobile-menu-toggle {
      display: flex;
    }
  }

  @media (max-width: 480px) {
    .header {
      padding: 0 1rem;
    }

    .nav {
      height: 65px;
    }

    .logo {
      font-size: 1.125rem;
    }

    #header-logo-img {
      height: 32px !important;
      max-width: 120px !important;
    }

    .nav-links {
      top: 65px;
      padding: 1.5rem 1rem;
    }

    /* Extra compact CTA for smallest screens */
    .header-cta {
      padding: 0.45rem 0.75rem;
      font-size: 0.9rem;
      gap: 0.3rem;
    }

    .cta-icon {
      width: 14px;
      height: 14px;
    }

    .nav-actions {
      gap: 0.5rem;
    }
  }

  /* Theme Toggle */
  .theme-toggle {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: none;
    background: var(--color-bg-alt);
    color: var(--color-text);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--transition-fast);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .theme-toggle:hover {
    background: var(--color-primary);
    color: white;
    transform: scale(1.1);
    box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
  }

  .theme-toggle:active {
    transform: scale(0.95);
  }

  [data-theme="dark"] .sun-icon {
    display: none;
  }

  [data-theme="dark"] .moon-icon {
    display: block !important;
  }

  @media (max-width: 768px) {
    .theme-toggle {
      width: 36px;
      height: 36px;
    }
  }
</style>

<script>
  function initMobileMenu() {
    const toggle = document.querySelector('.mobile-menu-toggle');
    const menu = document.querySelector('.nav-links');

    if (toggle && menu) {
      toggle.addEventListener('click', () => {
        menu.classList.toggle('active');
        toggle.classList.toggle('active');
      });
    }

    // Mobile dropdown toggle
    const dropdownTriggers = document.querySelectorAll('.nav-dropdown .dropdown-trigger');
    dropdownTriggers.forEach(trigger => {
      trigger.addEventListener('click', (e) => {
        if (window.innerWidth <= 768) {
          e.preventDefault();
          const parent = trigger.closest('.nav-dropdown');
          parent?.classList.toggle('active');
        }
      });
    });
  }

  initMobileMenu();
  document.addEventListener('astro:page-load', initMobileMenu);

  // Load dynamic site name and logo from API with sessionStorage caching
  async function loadSiteName() {
    try {
      // Check cache first (10 minute TTL)
      const cacheKey = 'header-site-content';
      const cached = sessionStorage.getItem(cacheKey);
      let data;

      if (cached) {
        const { content, timestamp } = JSON.parse(cached);
        const now = Date.now();

        // Use cache if less than 10 minutes old
        if (now - timestamp < 600000) {
          data = content;
        }
      }

      // Fetch from API if no cache or expired
      if (!data) {
        const response = await fetch('/api/admin/content');
        if (response.ok) {
          const apiData = await response.json();
          if (apiData.success && apiData.content) {
            data = apiData.content;
            // Cache for 10 minutes
            sessionStorage.setItem(cacheKey, JSON.stringify({
              content: data,
              timestamp: Date.now()
            }));
          }
        }
      }

      // Apply cached or fetched data
      if (data) {
        // Update site name
        if (data.site_name) {
          const siteNameEl = document.getElementById('header-site-name');
          if (siteNameEl) {
            siteNameEl.textContent = data.site_name;
          }
        }

        // Update logo if available
        if (data.site_logo && data.site_logo.trim() !== '') {
          const logoImg = document.getElementById('header-logo-img') as HTMLImageElement;
          const logoText = document.getElementById('header-site-name');

          if (logoImg) {
            logoImg.src = data.site_logo;
            logoImg.style.display = 'block';

            // Hide text logo when image logo is shown
            if (logoText) {
              logoText.style.display = 'none';
            }

            // Fallback to text if image fails to load
            logoImg.onerror = () => {
              logoImg.style.display = 'none';
              if (logoText) {
                logoText.style.display = 'inline';
              }
            };
          }
        }
      }
    } catch (error) {
      console.error('Error loading site name:', error);
    }
  }

  loadSiteName();
  document.addEventListener('astro:page-load', loadSiteName);

  // Load dynamic blog post count with sessionStorage caching
  async function loadBlogPostCount() {
    try {
      // Check cache first (10 minute TTL)
      const cacheKey = 'header-blog-count';
      const cached = sessionStorage.getItem(cacheKey);
      let count = 0;

      if (cached) {
        const { blogCount, timestamp } = JSON.parse(cached);
        const now = Date.now();

        // Use cache if less than 10 minutes old
        if (now - timestamp < 600000) {
          count = blogCount;
        }
      }

      // Fetch from API if no cache or expired
      if (!cached || count === 0) {
        const response = await fetch('/api/admin/blog');

        if (response.ok) {
          const data = await response.json();

          if (data.success && data.posts) {
            count = data.posts.length;
            // Cache for 10 minutes
            sessionStorage.setItem(cacheKey, JSON.stringify({
              blogCount: count,
              timestamp: Date.now()
            }));
          }
        }
      }

      // Update UI with cached or fetched count
      const counter = document.getElementById('blog-post-counter');
      const dropdownCounter = document.getElementById('blog-post-counter-dropdown');

      if (counter) {
        counter.textContent = count.toString();
        counter.style.display = 'flex';
      }
      if (dropdownCounter) {
        dropdownCounter.textContent = count.toString();
        dropdownCounter.style.display = 'inline-flex';
      }
    } catch (error) {
      console.error('Error loading blog post count:', error);
      const counter = document.getElementById('blog-post-counter');
      const dropdownCounter = document.getElementById('blog-post-counter-dropdown');

      if (counter) {
        counter.textContent = '0';
        counter.style.display = 'flex';
      }
      if (dropdownCounter) {
        dropdownCounter.textContent = '0';
        dropdownCounter.style.display = 'inline-flex';
      }
    }
  }

  loadBlogPostCount();
  document.addEventListener('astro:page-load', loadBlogPostCount);

  // Theme Toggle Functionality
  function initThemeToggle() {
    const themeToggle = document.getElementById('theme-toggle');
    const html = document.documentElement;

    // Check for saved theme preference or default to dark mode
    const currentTheme = localStorage.getItem('theme') || 'dark';
    html.setAttribute('data-theme', currentTheme);

    if (themeToggle) {
      themeToggle.addEventListener('click', () => {
        const currentTheme = html.getAttribute('data-theme');
        const newTheme = currentTheme === 'light' ? 'dark' : 'light';

        html.setAttribute('data-theme', newTheme);
        localStorage.setItem('theme', newTheme);
      });
    }
  }

  initThemeToggle();
  document.addEventListener('astro:page-load', initThemeToggle);
</script>
