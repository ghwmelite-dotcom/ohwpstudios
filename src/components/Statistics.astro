---
// Statistics Section with Animated Counters
// World-class statistics display with counting animations
---

<section id="statistics" class="section statistics-section" data-animate>
  <div class="container">
    <!-- Section Header -->
    <div class="section-header" style="text-align: center; max-width: 700px; margin: 0 auto 4rem;">
      <span class="badge" style="display: inline-block; padding: 0.5rem 1.5rem; background: var(--gradient-primary); color: white; border-radius: var(--radius-full); font-weight: 600; font-size: 0.875rem; margin-bottom: 1rem;">
        Fun Facts
      </span>
      <h2 data-text-reveal class="title-hero stats-title">
        <span class="title-number">📊</span>
        <span class="title-gradient">The Numbers Speak For Themselves</span>
        <span class="title-number">📊</span>
        <div class="title-underline"></div>
      </h2>
      <p style="font-size: 1.25rem; margin-top: 1rem;">
        Our journey in numbers - a testament to our commitment and success
      </p>
    </div>

    <!-- Statistics Grid -->
    <div class="stats-grid">

      <!-- Stat Card 1 -->
      <div class="stat-card card" data-tilt data-stagger>
        <div class="stat-icon-wrapper" style="background: linear-gradient(135deg, #6366f1 0%, #818cf8 100%);">
          <svg class="stat-icon" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
            <circle cx="9" cy="7" r="4"></circle>
            <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
            <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
          </svg>
        </div>
        <div class="stat-number">
          <span data-counter="400" data-suffix="+">0</span>
        </div>
        <div class="stat-label">Happy Clients</div>
        <p class="stat-description">
          Businesses trust us with their digital presence
        </p>
        <div class="stat-glow" style="background: radial-gradient(circle, rgba(99, 102, 241, 0.2), transparent);"></div>
      </div>

      <!-- Stat Card 2 -->
      <div class="stat-card card" data-tilt data-stagger>
        <div class="stat-icon-wrapper" style="background: linear-gradient(135deg, #0073aa 0%, #00a0d2 100%);">
          <svg class="stat-icon" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
          </svg>
        </div>
        <div class="stat-number">
          <span data-counter="500" data-suffix="+">0</span>
        </div>
        <div class="stat-label">Projects Completed</div>
        <p class="stat-description">
          Successfully delivered and launched worldwide
        </p>
        <div class="stat-glow" style="background: radial-gradient(circle, rgba(0, 115, 170, 0.2), transparent);"></div>
      </div>

      <!-- Stat Card 3 -->
      <div class="stat-card card" data-tilt data-stagger>
        <div class="stat-icon-wrapper" style="background: linear-gradient(135deg, #ec4899 0%, #f59e0b 100%);">
          <svg class="stat-icon" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
            <path d="M18 8h1a4 4 0 0 1 0 8h-1"></path>
            <path d="M2 8h16v9a4 4 0 0 1-4 4H6a4 4 0 0 1-4-4V8z"></path>
            <line x1="6" y1="1" x2="6" y2="4"></line>
            <line x1="10" y1="1" x2="10" y2="4"></line>
            <line x1="14" y1="1" x2="14" y2="4"></line>
          </svg>
        </div>
        <div class="stat-number">
          <span data-counter="1000" data-suffix="+">0</span>
        </div>
        <div class="stat-label">Cups of Coffee</div>
        <p class="stat-description">
          Fuel for our creative problem-solving sessions
        </p>
        <div class="stat-glow" style="background: radial-gradient(circle, rgba(236, 72, 153, 0.2), transparent);"></div>
      </div>

      <!-- Stat Card 4 -->
      <div class="stat-card card" data-tilt data-stagger>
        <div class="stat-icon-wrapper" style="background: linear-gradient(135deg, #8b5cf6 0%, #6366f1 100%);">
          <svg class="stat-icon" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
            <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
          </svg>
        </div>
        <div class="stat-number">
          <span data-counter="100" data-suffix="+">0</span>
        </div>
        <div class="stat-label">Innovations</div>
        <p class="stat-description">
          Cutting-edge features and creative solutions
        </p>
        <div class="stat-glow" style="background: radial-gradient(circle, rgba(139, 92, 246, 0.2), transparent);"></div>
      </div>

    </div>

    <!-- Achievement Statement -->
    <div class="achievement-card glass" data-animate style="margin-top: 4rem; padding: 3rem; border-radius: var(--radius-2xl); text-align: center; position: relative; overflow: hidden;">
      <div class="achievement-bg" style="position: absolute; inset: 0; background: var(--gradient-mesh); opacity: 0.3; pointer-events: none;"></div>
      <div style="position: relative; z-index: 1;">
        <div style="font-size: 3rem; margin-bottom: 1rem;">🏆</div>
        <h3 style="font-size: 2rem; margin-bottom: 1rem;">Building Digital Excellence Since 2014</h3>
        <p style="font-size: 1.25rem; color: var(--color-text-muted); max-width: 700px; margin: 0 auto;">
          Every project is an opportunity to push boundaries and deliver exceptional results that exceed expectations. Our track record speaks for itself.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .statistics-section {
    background: linear-gradient(180deg, var(--color-bg) 0%, var(--color-bg-alt) 100%);
    position: relative;
    overflow: hidden;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .stat-card {
    padding: 2.5rem 2rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    transition: all 0.4s var(--ease-smooth);
    cursor: default;
  }

  .stat-card:hover {
    transform: translateY(-10px) scale(1.02);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .stat-card:hover .stat-glow {
    opacity: 1;
    transform: scale(1.5);
  }

  .stat-card:hover .stat-icon-wrapper {
    transform: scale(1.1) rotate(5deg);
  }

  .stat-icon-wrapper {
    width: 80px;
    height: 80px;
    margin: 0 auto 1.5rem;
    border-radius: var(--radius-xl);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.4s var(--ease-spring);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  }

  .stat-icon {
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.2));
  }

  .stat-number {
    font-size: 4rem;
    font-weight: 800;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    margin-bottom: 0.75rem;
    font-family: var(--font-display);
  }

  .stat-label {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 0.75rem;
  }

  .stat-description {
    font-size: 0.9375rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  .stat-glow {
    position: absolute;
    width: 200px;
    height: 200px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
    opacity: 0;
    transition: all 0.6s var(--ease-smooth);
    border-radius: 50%;
    filter: blur(60px);
  }

  .achievement-card {
    border: 1px solid var(--color-border);
    transition: all 0.4s var(--ease-smooth);
  }

  .achievement-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  .achievement-bg {
    animation: gradient-shift 15s ease infinite;
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1.5rem;
    }

    .stat-card {
      padding: 2rem 1.5rem;
    }

    .stat-number {
      font-size: 3rem;
    }

    .stat-icon-wrapper {
      width: 70px;
      height: 70px;
    }

    .achievement-card {
      padding: 2rem !important;
    }

    .achievement-card h3 {
      font-size: 1.5rem !important;
    }

    .achievement-card p {
      font-size: 1rem !important;
    }
  }

  @media (max-width: 480px) {
    .stats-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Beautiful Animated Title Styles */
  .title-hero {
    position: relative;
    display: inline-block;
    padding-bottom: 1rem;
  }

  .stats-title {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .title-gradient {
    background: linear-gradient(135deg, #6366f1 0%, #ec4899 50%, #f59e0b 100%);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 4s ease infinite;
    display: inline-block;
    font-weight: 800;
  }

  @keyframes gradientShift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .title-number {
    display: inline-block;
    animation: numberPulse 2s ease-in-out infinite;
    font-size: 2rem;
  }

  @keyframes numberPulse {
    0%, 100% {
      transform: scale(1);
      opacity: 1;
    }
    50% {
      transform: scale(1.2);
      opacity: 0.8;
    }
  }

  .title-underline {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 4px;
    background: linear-gradient(90deg, transparent, #6366f1, #ec4899, #f59e0b, transparent);
    border-radius: 2px;
    animation: underlineExpand 1.5s ease-out forwards 0.5s;
    box-shadow: 0 2px 10px rgba(99, 102, 241, 0.5);
  }

  @keyframes underlineExpand {
    to {
      width: 80%;
    }
  }
</style>
