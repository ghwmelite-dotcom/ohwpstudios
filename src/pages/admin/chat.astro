---
export const prerender = false;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Live Chat - Hodges & Co. Admin</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', system-ui, sans-serif;
      background: linear-gradient(135deg, #f0f4ff 0%, #fef3f9 50%, #f9fafb 100%);
      background-attachment: fixed;
      color: #1a1a2e;
      overflow: hidden;
    }

    .admin-nav {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(227, 169, 43, 0.1);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 4px 24px rgba(227, 169, 43, 0.08);
      position: relative;
      z-index: 100;
    }

    .nav-brand {
      font-size: 1.375rem;
      font-weight: 900;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 50%, #F5C969 100%);
      background-size: 200% 200%;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: gradient-flow 4s ease infinite;
      letter-spacing: -0.02em;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .nav-brand::before {
      content: '💬';
      font-size: 1.5rem;
    }

    @keyframes gradient-flow {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }

    .nav-links {
      display: flex;
      gap: 1.5rem;
    }

    .nav-links a {
      color: #64748b;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      padding: 0.5rem 1rem;
      border-radius: 8px;
      position: relative;
    }

    .nav-links a::before {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      width: 0;
      height: 2px;
      background: linear-gradient(135deg, #E3A92B, #F5C969);
      transform: translateX(-50%);
      transition: width 0.3s ease;
    }

    .nav-links a:hover {
      color: #E3A92B;
      background: rgba(227, 169, 43, 0.05);
    }

    .nav-links a:hover::before {
      width: 60%;
    }

    .nav-links a.active {
      color: #E3A92B;
      background: linear-gradient(135deg, rgba(227, 169, 43, 0.1), rgba(245, 201, 105, 0.1));
      font-weight: 700;
    }

    .nav-links a.active::before {
      width: 60%;
    }

    .chat-container {
      display: grid;
      grid-template-columns: 380px 1fr;
      height: calc(100vh - 73px);
      background: transparent;
      gap: 1.5rem;
      padding: 1.5rem;
      padding-top: 1rem;
    }

    /* Conversations Sidebar */
    .conversations-sidebar {
      border-right: none;
      display: flex;
      flex-direction: column;
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(227, 169, 43, 0.12);
      border: 1px solid rgba(255, 255, 255, 0.8);
      overflow: hidden;
    }

    .sidebar-header {
      padding: 1.75rem;
      border-bottom: 1px solid rgba(227, 169, 43, 0.1);
      background: linear-gradient(135deg, rgba(227, 169, 43, 0.03), rgba(245, 201, 105, 0.03));
    }

    .header-title-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1.25rem;
    }

    .sidebar-header h2 {
      font-size: 1.375rem;
      margin-bottom: 0;
      font-weight: 800;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 50%, #F5C969 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      letter-spacing: -0.02em;
    }

    .live-indicator {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.375rem 0.75rem;
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(5, 150, 105, 0.1));
      border-radius: 20px;
      border: 1px solid rgba(16, 185, 129, 0.2);
    }

    .live-dot {
      width: 8px;
      height: 8px;
      background: #10b981;
      border-radius: 50%;
      animation: pulse-live 2s ease-in-out infinite;
      box-shadow: 0 0 8px rgba(16, 185, 129, 0.6);
    }

    @keyframes pulse-live {
      0%, 100% {
        opacity: 1;
        transform: scale(1);
      }
      50% {
        opacity: 0.7;
        transform: scale(1.2);
      }
    }

    .live-text {
      font-size: 0.75rem;
      font-weight: 700;
      color: #10b981;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .search-container {
      position: relative;
      margin-bottom: 1.25rem;
    }

    .search-icon {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      color: #94a3b8;
      pointer-events: none;
      z-index: 1;
    }

    .search-input {
      width: 100%;
      padding: 0.75rem 1rem 0.75rem 2.75rem;
      border: 2px solid rgba(227, 169, 43, 0.15);
      border-radius: 12px;
      font-size: 0.875rem;
      font-family: inherit;
      background: rgba(255, 255, 255, 0.8);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      color: #1a1a2e;
    }

    .search-input::placeholder {
      color: #94a3b8;
    }

    .search-input:focus {
      outline: none;
      border-color: #E3A92B;
      background: white;
      box-shadow: 0 0 0 4px rgba(227, 169, 43, 0.1);
    }

    .search-input:focus + .search-icon {
      color: #E3A92B;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 0.625rem;
      margin-bottom: 1.25rem;
    }

    .stat-card {
      padding: 0.875rem;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 12px;
      color: white;
      position: relative;
      overflow: hidden;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      border: 2px solid rgba(227, 169, 43, 0.1);
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .stat-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(227, 169, 43, 0.05), rgba(245, 201, 105, 0.05));
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .stat-card:hover {
      transform: translateY(-2px);
      border-color: rgba(227, 169, 43, 0.3);
      box-shadow: 0 4px 16px rgba(227, 169, 43, 0.15);
    }

    .stat-card:hover::before {
      opacity: 1;
    }

    .stat-icon {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      position: relative;
      z-index: 1;
      box-shadow: 0 4px 12px rgba(227, 169, 43, 0.25);
    }

    .stat-card.stat-active .stat-icon {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.25);
    }

    .stat-card.stat-pending .stat-icon {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
      box-shadow: 0 4px 12px rgba(245, 158, 11, 0.25);
    }

    .stat-card.stat-total .stat-icon {
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      box-shadow: 0 4px 12px rgba(227, 169, 43, 0.25);
    }

    .stat-icon svg {
      position: relative;
      z-index: 1;
    }

    .stat-content {
      flex: 1;
      position: relative;
      z-index: 1;
    }

    .stat-label {
      font-size: 0.6875rem;
      opacity: 0.7;
      margin-bottom: 0.25rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: #64748b;
    }

    .stat-value {
      font-size: 1.5rem;
      font-weight: 900;
      letter-spacing: -0.02em;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .stat-card.stat-active .stat-value {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .stat-card.stat-pending .stat-value {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .filter-tabs {
      display: flex;
      gap: 0.5rem;
      background: rgba(255, 255, 255, 0.5);
      padding: 0.375rem;
      border-radius: 10px;
      border: 1px solid rgba(227, 169, 43, 0.1);
    }

    .filter-tab {
      flex: 1;
      padding: 0.625rem 0.75rem;
      background: transparent;
      border: none;
      border-radius: 8px;
      font-size: 0.8125rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      color: #64748b;
      position: relative;
    }

    .filter-tab:hover {
      color: #E3A92B;
      background: rgba(227, 169, 43, 0.08);
    }

    .filter-tab.active {
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(227, 169, 43, 0.3);
      transform: translateY(-1px);
    }

    .conversations-list {
      flex: 1;
      overflow-y: auto;
      padding: 0.75rem;
      padding-top: 0.5rem;
    }

    .conversations-list::-webkit-scrollbar {
      width: 6px;
    }

    .conversations-list::-webkit-scrollbar-track {
      background: transparent;
    }

    .conversations-list::-webkit-scrollbar-thumb {
      background: rgba(227, 169, 43, 0.2);
      border-radius: 10px;
    }

    .conversations-list::-webkit-scrollbar-thumb:hover {
      background: rgba(227, 169, 43, 0.3);
    }

    .conversation-item {
      padding: 1rem;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 12px;
      margin-bottom: 0.625rem;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      border: 2px solid transparent;
      position: relative;
      overflow: hidden;
      display: flex;
      gap: 0.875rem;
      align-items: flex-start;
    }

    .conversation-item::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 4px;
      height: 100%;
      background: linear-gradient(135deg, #E3A92B, #F5C969);
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .conversation-item:hover {
      box-shadow: 0 4px 16px rgba(227, 169, 43, 0.15);
      transform: translateX(4px);
      background: white;
    }

    .conversation-item:hover::before {
      opacity: 0.6;
    }

    .conversation-item.active {
      border-color: transparent;
      background: linear-gradient(135deg, rgba(227, 169, 43, 0.1), rgba(245, 201, 105, 0.05));
      box-shadow: 0 4px 20px rgba(227, 169, 43, 0.2);
      transform: translateX(4px);
    }

    .conversation-item.active::before {
      opacity: 1;
    }

    .conversation-avatar-wrapper {
      position: relative;
      flex-shrink: 0;
    }

    .conversation-avatar {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 50%, #F5C969 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 800;
      font-size: 1.125rem;
      border: 3px solid rgba(255, 255, 255, 0.9);
      box-shadow: 0 4px 12px rgba(227, 169, 43, 0.25);
      position: relative;
      z-index: 1;
    }

    .conversation-online-dot {
      position: absolute;
      bottom: 2px;
      right: 2px;
      width: 12px;
      height: 12px;
      background: #10b981;
      border-radius: 50%;
      border: 2px solid white;
      box-shadow: 0 2px 8px rgba(16, 185, 129, 0.4);
      animation: pulse-online-dot 2s ease-in-out infinite;
      z-index: 2;
    }

    @keyframes pulse-online-dot {
      0%, 100% {
        opacity: 1;
        transform: scale(1);
      }
      50% {
        opacity: 0.8;
        transform: scale(1.1);
      }
    }

    .conversation-content {
      flex: 1;
      min-width: 0;
      position: relative;
      z-index: 1;
    }

    .conversation-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 0.5rem;
    }

    .conversation-name {
      font-weight: 700;
      font-size: 0.9375rem;
      color: #1a1a2e;
      letter-spacing: -0.01em;
    }

    .conversation-time {
      font-size: 0.75rem;
      color: #94a3b8;
      font-weight: 500;
    }

    .conversation-time-badge {
      font-size: 0.6875rem;
      color: #94a3b8;
      font-weight: 600;
      background: rgba(227, 169, 43, 0.08);
      padding: 0.25rem 0.5rem;
      border-radius: 6px;
      white-space: nowrap;
    }

    .conversation-preview {
      font-size: 0.875rem;
      color: #64748b;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      margin-bottom: 0.625rem;
      line-height: 1.4;
    }

    .conversation-meta {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .conversation-status {
      font-size: 0.6875rem;
      padding: 0.375rem 0.625rem;
      border-radius: 6px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .status-active {
      background: linear-gradient(135deg, #dcfce7, #bbf7d0);
      color: #16a34a;
      border: 1px solid rgba(22, 163, 74, 0.2);
    }

    .status-pending {
      background: linear-gradient(135deg, #fef3c7, #fde68a);
      color: #d97706;
      border: 1px solid rgba(217, 119, 6, 0.2);
    }

    .status-resolved {
      background: linear-gradient(135deg, #e0e7ff, #c7d2fe);
      color: #B8860B;
      border: 1px solid rgba(79, 70, 229, 0.2);
    }

    .unread-badge {
      background: linear-gradient(135deg, #ef4444, #dc2626);
      color: white;
      font-size: 0.6875rem;
      padding: 0.25rem 0.625rem;
      border-radius: 12px;
      font-weight: 700;
      box-shadow: 0 2px 8px rgba(239, 68, 68, 0.3);
      animation: pulse-badge 2s ease-in-out infinite;
    }

    @keyframes pulse-badge {
      0%, 100% {
        transform: scale(1);
        box-shadow: 0 2px 8px rgba(239, 68, 68, 0.3);
      }
      50% {
        transform: scale(1.05);
        box-shadow: 0 4px 12px rgba(239, 68, 68, 0.4);
      }
    }

    /* Chat Area */
    .chat-area {
      display: flex;
      flex-direction: column;
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(227, 169, 43, 0.12);
      border: 1px solid rgba(255, 255, 255, 0.8);
      overflow: hidden;
    }

    .chat-header {
      padding: 1.75rem 2rem;
      border-bottom: 1px solid rgba(227, 169, 43, 0.1);
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: linear-gradient(135deg, rgba(227, 169, 43, 0.02), rgba(245, 201, 105, 0.02));
    }

    .chat-header-info {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .chat-avatar {
      width: 56px;
      height: 56px;
      border-radius: 50%;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 50%, #F5C969 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 800;
      font-size: 1.375rem;
      box-shadow: 0 4px 16px rgba(227, 169, 43, 0.3);
      border: 3px solid rgba(255, 255, 255, 0.9);
      position: relative;
    }

    .chat-avatar::after {
      content: '';
      position: absolute;
      bottom: 2px;
      right: 2px;
      width: 14px;
      height: 14px;
      background: #16a34a;
      border-radius: 50%;
      border: 2px solid white;
      box-shadow: 0 2px 8px rgba(22, 163, 74, 0.4);
    }

    .chat-visitor-details h3 {
      font-size: 1.125rem;
      margin-bottom: 0.25rem;
      font-weight: 700;
      color: #1a1a2e;
      letter-spacing: -0.01em;
    }

    .chat-visitor-email {
      font-size: 0.875rem;
      color: #64748b;
      font-weight: 500;
    }

    .chat-actions {
      display: flex;
      gap: 0.5rem;
    }

    .btn {
      padding: 0.625rem 1.25rem;
      border-radius: 10px;
      font-size: 0.875rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      border: none;
      position: relative;
      overflow: hidden;
    }

    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(255, 255, 255, 0.2);
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .btn:hover::before {
      opacity: 1;
    }

    .btn-primary {
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(227, 169, 43, 0.2);
    }

    .btn-primary:hover {
      box-shadow: 0 6px 20px rgba(227, 169, 43, 0.35);
      transform: translateY(-1px);
    }

    .btn-secondary {
      background: rgba(255, 255, 255, 0.9);
      border: 2px solid rgba(227, 169, 43, 0.2);
      color: #E3A92B;
    }

    .btn-secondary:hover {
      border-color: #E3A92B;
      background: rgba(227, 169, 43, 0.05);
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(227, 169, 43, 0.15);
    }

    .chat-messages {
      flex: 1;
      overflow-y: auto;
      padding: 2rem;
      background: linear-gradient(135deg, rgba(249, 250, 251, 0.5), rgba(240, 244, 255, 0.3));
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
    }

    .chat-messages::-webkit-scrollbar {
      width: 6px;
    }

    .chat-messages::-webkit-scrollbar-track {
      background: transparent;
    }

    .chat-messages::-webkit-scrollbar-thumb {
      background: rgba(227, 169, 43, 0.2);
      border-radius: 10px;
    }

    .chat-messages::-webkit-scrollbar-thumb:hover {
      background: rgba(227, 169, 43, 0.3);
    }

    .message {
      display: flex;
      gap: 0.75rem;
      max-width: 70%;
      animation: fadeIn 0.3s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .message.visitor {
      align-self: flex-start;
    }

    .message.operator {
      align-self: flex-end;
      flex-direction: row-reverse;
    }

    .message-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: linear-gradient(135deg, #e5e7eb, #f1f5f9);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 0.9375rem;
      flex-shrink: 0;
      border: 2px solid rgba(255, 255, 255, 0.9);
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
      color: #64748b;
    }

    .message.operator .message-avatar {
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      color: white;
      box-shadow: 0 2px 12px rgba(227, 169, 43, 0.3);
    }

    .message-content {
      flex: 1;
    }

    .message-bubble {
      padding: 0.9375rem 1.25rem;
      border-radius: 16px;
      font-size: 0.9375rem;
      line-height: 1.6;
      position: relative;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
    }

    .message.visitor .message-bubble {
      background: rgba(255, 255, 255, 0.95);
      border: 1px solid rgba(227, 169, 43, 0.1);
      border-radius: 16px 16px 16px 4px;
      color: #1a1a2e;
    }

    .message.operator .message-bubble {
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      color: white;
      border-radius: 16px 16px 4px 16px;
      box-shadow: 0 4px 16px rgba(227, 169, 43, 0.25);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .message-time {
      font-size: 0.75rem;
      color: #94a3b8;
      margin-top: 0.25rem;
      padding: 0 0.25rem;
    }

    .message.operator .message-time {
      text-align: right;
    }

    .chat-input-area {
      padding: 1.75rem 2rem;
      border-top: 1px solid rgba(227, 169, 43, 0.1);
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.95), rgba(249, 250, 251, 0.95));
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .canned-responses {
      display: flex;
      gap: 0.625rem;
      margin-bottom: 1.125rem;
      flex-wrap: wrap;
    }

    .canned-btn {
      padding: 0.625rem 0.875rem;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(227, 169, 43, 0.2);
      border-radius: 8px;
      font-size: 0.8125rem;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      font-weight: 500;
      color: #64748b;
    }

    .canned-btn:hover {
      background: linear-gradient(135deg, rgba(227, 169, 43, 0.08), rgba(227, 169, 43, 0.08));
      border-color: #E3A92B;
      color: #E3A92B;
      transform: translateY(-1px);
      box-shadow: 0 2px 8px rgba(227, 169, 43, 0.15);
    }

    .chat-input-form {
      display: flex;
      gap: 0.75rem;
    }

    #message-input {
      flex: 1;
      padding: 0.875rem 1.25rem;
      border: 2px solid rgba(227, 169, 43, 0.2);
      border-radius: 16px;
      font-size: 0.9375rem;
      font-family: inherit;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      background: rgba(255, 255, 255, 0.95);
      color: #1a1a2e;
    }

    #message-input::placeholder {
      color: #94a3b8;
    }

    #message-input:focus {
      outline: none;
      border-color: #E3A92B;
      box-shadow: 0 0 0 4px rgba(227, 169, 43, 0.1), 0 4px 16px rgba(227, 169, 43, 0.15);
      background: white;
    }

    .send-btn {
      width: 52px;
      height: 52px;
      border-radius: 50%;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 100%);
      border: none;
      color: white;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 16px rgba(227, 169, 43, 0.3);
      position: relative;
      overflow: hidden;
    }

    .send-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: radial-gradient(circle, rgba(255, 255, 255, 0.2), transparent);
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .send-btn:hover {
      transform: scale(1.08) rotate(15deg);
      box-shadow: 0 6px 24px rgba(227, 169, 43, 0.45);
    }

    .send-btn:hover::before {
      opacity: 1;
    }

    .send-btn:active {
      transform: scale(1);
    }

    .empty-state {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100%;
      color: #94a3b8;
      text-align: center;
      padding: 3rem;
    }

    .empty-icon {
      width: 100px;
      height: 100px;
      background: linear-gradient(135deg, #E3A92B 0%, #E3A92B 50%, #F5C969 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.5rem;
      color: white;
      box-shadow: 0 8px 32px rgba(227, 169, 43, 0.3);
      position: relative;
      animation: float-icon 3s ease-in-out infinite;
    }

    .empty-icon::before {
      content: '';
      position: absolute;
      inset: -4px;
      border-radius: 50%;
      background: linear-gradient(135deg, #E3A92B, #F5C969);
      opacity: 0.2;
      filter: blur(10px);
      animation: pulse-ring 2s ease-in-out infinite;
    }

    @keyframes float-icon {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }

    @keyframes pulse-ring {
      0%, 100% {
        opacity: 0.2;
        transform: scale(1);
      }
      50% {
        opacity: 0.3;
        transform: scale(1.1);
      }
    }

    .empty-state h3 {
      font-size: 1.375rem;
      margin-bottom: 0.625rem;
      color: #1a1a2e;
      font-weight: 700;
    }

    .empty-state p {
      font-size: 0.9375rem;
      color: #64748b;
    }

    .loading {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2.5rem;
      color: #94a3b8;
    }

    .spinner {
      width: 32px;
      height: 32px;
      border: 3px solid rgba(227, 169, 43, 0.1);
      border-top-color: #E3A92B;
      border-right-color: #E3A92B;
      border-radius: 50%;
      animation: spin 0.8s ease-in-out infinite;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }
  </style>
</head>
<body>
  <!-- Navigation -->
  <nav class="admin-nav">
    <div class="nav-brand">Hodges & Co. Admin</div>
    <div class="nav-links">
      <a href="/admin/dashboard">Dashboard</a>
      <a href="/admin/chat" class="active">Live Chat</a>
      <a href="/admin/portfolio">Portfolio</a>
      <a href="/admin/blog">Blog</a>
      <a href="/">View Site</a>
    </div>
  </nav>

  <!-- Chat Container -->
  <div class="chat-container">
    <!-- Conversations Sidebar -->
    <div class="conversations-sidebar">
      <div class="sidebar-header">
        <div class="header-title-row">
          <h2>Conversations</h2>
          <div class="live-indicator">
            <span class="live-dot"></span>
            <span class="live-text">Live</span>
          </div>
        </div>

        <!-- Search Bar -->
        <div class="search-container">
          <svg class="search-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="11" cy="11" r="8"></circle>
            <path d="m21 21-4.35-4.35"></path>
          </svg>
          <input
            type="text"
            id="search-conversations"
            class="search-input"
            placeholder="Search conversations..."
            autocomplete="off"
          />
        </div>

        <!-- Stats -->
        <div class="stats-grid">
          <div class="stat-card stat-active">
            <div class="stat-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
              </svg>
            </div>
            <div class="stat-content">
              <div class="stat-label">Active</div>
              <div class="stat-value" id="stat-active">0</div>
            </div>
          </div>
          <div class="stat-card stat-pending">
            <div class="stat-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
              </svg>
            </div>
            <div class="stat-content">
              <div class="stat-label">Pending</div>
              <div class="stat-value" id="stat-pending">0</div>
            </div>
          </div>
          <div class="stat-card stat-total">
            <div class="stat-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
              </svg>
            </div>
            <div class="stat-content">
              <div class="stat-label">Total</div>
              <div class="stat-value" id="stat-total">0</div>
            </div>
          </div>
        </div>

        <!-- Filters -->
        <div class="filter-tabs">
          <button class="filter-tab active" data-status="all">
            <span>All</span>
          </button>
          <button class="filter-tab" data-status="active">
            <span>Active</span>
          </button>
          <button class="filter-tab" data-status="pending">
            <span>Pending</span>
          </button>
          <button class="filter-tab" data-status="resolved">
            <span>Resolved</span>
          </button>
        </div>
      </div>

      <!-- Conversations List -->
      <div class="conversations-list" id="conversations-list">
        <div class="loading">
          <div class="spinner"></div>
        </div>
      </div>
    </div>

    <!-- Chat Area -->
    <div class="chat-area">
      <div class="empty-state" id="empty-state">
        <div class="empty-icon">
          <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
          </svg>
        </div>
        <h3>No conversation selected</h3>
        <p>Select a conversation from the list to view messages</p>
      </div>

      <!-- Active Chat (hidden by default) -->
      <div id="active-chat" style="display: none; height: 100%; display: flex; flex-direction: column;">
        <div class="chat-header">
          <div class="chat-header-info">
            <div class="chat-avatar" id="chat-avatar">V</div>
            <div class="chat-visitor-details">
              <h3 id="chat-visitor-name">-</h3>
              <div class="chat-visitor-email" id="chat-visitor-email">-</div>
            </div>
          </div>
          <div class="chat-actions">
            <button class="btn btn-secondary" id="resolve-btn">Mark as Resolved</button>
            <button class="btn btn-primary" id="assign-btn">Assign to Me</button>
          </div>
        </div>

        <div class="chat-messages" id="chat-messages">
          <!-- Messages will be inserted here -->
        </div>

        <div class="chat-input-area">
          <div class="canned-responses" id="canned-responses">
            <!-- Canned responses will be inserted here -->
          </div>

          <form class="chat-input-form" id="message-form">
            <input
              type="text"
              id="message-input"
              placeholder="Type your message..."
              autocomplete="off"
              required
            />
            <button type="submit" class="send-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="22" y1="2" x2="11" y2="13"></line>
                <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
              </svg>
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <script>
    // State
    let conversations: any[] = [];
    let activeConversation: any = null;
    let currentFilter = 'all';
    let pollingInterval: any = null;

    // Load conversations on page load
    loadConversations();
    loadCannedResponses();

    // Set up polling
    pollingInterval = setInterval(() => {
      if (activeConversation) {
        loadMessages(activeConversation.id, true);
      }
      loadConversations();
    }, 5000);

    // Search functionality
    const searchInput = document.getElementById('search-conversations') as HTMLInputElement;
    searchInput?.addEventListener('input', (e) => {
      renderConversations();
    });

    // Filter tabs
    document.querySelectorAll('.filter-tab').forEach(tab => {
      tab.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        document.querySelectorAll('.filter-tab').forEach(t => t.classList.remove('active'));
        target.classList.add('active');
        currentFilter = target.dataset.status || 'all';
        renderConversations();
      });
    });

    // Message form
    document.getElementById('message-form')?.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (!activeConversation) return;

      const input = document.getElementById('message-input') as HTMLInputElement;
      const message = input.value.trim();
      if (!message) return;

      try {
        const response = await fetch('/api/chat/messages', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            conversation_id: activeConversation.id,
            message: message,
            sender_type: 'operator',
            sender_name: 'Support Team'
          })
        });

        if (response.ok) {
          input.value = '';
          await loadMessages(activeConversation.id);
        }
      } catch (error) {
        console.error('Error sending message:', error);
      }
    });

    // Load conversations
    async function loadConversations() {
      try {
        const response = await fetch('/api/chat/conversations');
        const data = await response.json();

        if (data.success) {
          conversations = data.conversations;
          renderConversations();

          // Update stats
          if (data.stats) {
            document.getElementById('stat-active')!.textContent = data.stats.active || 0;
            document.getElementById('stat-pending')!.textContent = data.stats.pending || 0;
            document.getElementById('stat-total')!.textContent = data.stats.total || 0;
          }
        }
      } catch (error) {
        console.error('Error loading conversations:', error);
      }
    }

    // Render conversations
    function renderConversations() {
      const list = document.getElementById('conversations-list');
      if (!list) return;

      // Apply status filter
      let filtered = currentFilter === 'all'
        ? conversations
        : conversations.filter(c => c.status === currentFilter);

      // Apply search filter
      const searchInput = document.getElementById('search-conversations') as HTMLInputElement;
      const searchTerm = searchInput?.value.toLowerCase().trim();
      if (searchTerm) {
        filtered = filtered.filter(c =>
          (c.visitor_name || '').toLowerCase().includes(searchTerm) ||
          (c.visitor_email || '').toLowerCase().includes(searchTerm) ||
          (c.last_message || '').toLowerCase().includes(searchTerm)
        );
      }

      if (filtered.length === 0) {
        list.innerHTML = '<div class="loading" style="color: #94a3b8; padding: 2rem; text-align: center;">No conversations found</div>';
        return;
      }

      list.innerHTML = filtered.map(conv => {
        const initial = (conv.visitor_name || 'U').charAt(0).toUpperCase();
        const isOnline = conv.status === 'active';
        return `
        <div class="conversation-item ${activeConversation?.id === conv.id ? 'active' : ''}" data-id="${conv.id}">
          <div class="conversation-avatar-wrapper">
            <div class="conversation-avatar">${initial}</div>
            ${isOnline ? '<div class="conversation-online-dot"></div>' : ''}
          </div>
          <div class="conversation-content">
            <div class="conversation-header">
              <div class="conversation-name">${conv.visitor_name || 'Unknown Visitor'}</div>
              <div class="conversation-time-badge">${formatTime(conv.last_message_at)}</div>
            </div>
            <div class="conversation-preview">${conv.last_message || 'No messages yet'}</div>
            <div class="conversation-meta">
              <span class="conversation-status status-${conv.status}">${conv.status}</span>
              ${conv.unread_count > 0 ? `<span class="unread-badge">${conv.unread_count}</span>` : ''}
            </div>
          </div>
        </div>
      `}).join('');

      // Add click handlers
      list.querySelectorAll('.conversation-item').forEach(item => {
        item.addEventListener('click', () => {
          const id = parseInt(item.getAttribute('data-id') || '0');
          const conv = conversations.find(c => c.id === id);
          if (conv) {
            selectConversation(conv);
          }
        });
      });
    }

    // Select conversation
    function selectConversation(conv: any) {
      activeConversation = conv;
      renderConversations();

      // Show chat area
      document.getElementById('empty-state')!.style.display = 'none';
      const chatArea = document.getElementById('active-chat')!;
      chatArea.style.display = 'flex';

      // Update header
      const initial = (conv.visitor_name || 'V').charAt(0).toUpperCase();
      document.getElementById('chat-avatar')!.textContent = initial;
      document.getElementById('chat-visitor-name')!.textContent = conv.visitor_name || 'Unknown';
      document.getElementById('chat-visitor-email')!.textContent = conv.visitor_email || '';

      // Load messages
      loadMessages(conv.id);
    }

    // Load messages
    async function loadMessages(conversationId: number, polling = false) {
      try {
        const response = await fetch(`/api/chat/messages?conversation_id=${conversationId}`);
        const data = await response.json();

        if (data.success && data.messages) {
          if (!polling || data.messages.length > 0) {
            renderMessages(data.messages);
          }
        }
      } catch (error) {
        console.error('Error loading messages:', error);
      }
    }

    // Render messages
    function renderMessages(messages: any[]) {
      const container = document.getElementById('chat-messages');
      if (!container) return;

      container.innerHTML = messages.map(msg => {
        const initial = (msg.sender_name || 'U').charAt(0).toUpperCase();
        const time = new Date(msg.sent_at).toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit' });

        return `
          <div class="message ${msg.sender_type}">
            <div class="message-avatar">${initial}</div>
            <div class="message-content">
              <div class="message-bubble">${escapeHtml(msg.message_text)}</div>
              <div class="message-time">${time}</div>
            </div>
          </div>
        `;
      }).join('');

      // Scroll to bottom
      container.scrollTop = container.scrollHeight;
    }

    // Load canned responses
    async function loadCannedResponses() {
      try {
        const response = await fetch('/api/chat/canned-responses');
        const data = await response.json();

        if (data.success && data.responses) {
          const container = document.getElementById('canned-responses');
          if (container) {
            container.innerHTML = data.responses.slice(0, 6).map((resp: any) =>
              `<button class="canned-btn" data-message="${escapeHtml(resp.message)}">${resp.title}</button>`
            ).join('');

            // Add click handlers
            container.querySelectorAll('.canned-btn').forEach(btn => {
              btn.addEventListener('click', () => {
                const input = document.getElementById('message-input') as HTMLInputElement;
                input.value = btn.getAttribute('data-message') || '';
                input.focus();
              });
            });
          }
        }
      } catch (error) {
        console.error('Error loading canned responses:', error);
      }
    }

    // Helper functions
    function formatTime(timestamp: string): string {
      const date = new Date(timestamp);
      const now = new Date();
      const diff = now.getTime() - date.getTime();
      const minutes = Math.floor(diff / 60000);

      if (minutes < 1) return 'Just now';
      if (minutes < 60) return `${minutes}m ago`;
      if (minutes < 1440) return `${Math.floor(minutes / 60)}h ago`;
      return date.toLocaleDateString();
    }

    function escapeHtml(text: string): string {
      const div = document.createElement('div');
      div.textContent = text;
      return div.innerHTML;
    }

    // Cleanup on unload
    window.addEventListener('beforeunload', () => {
      if (pollingInterval) clearInterval(pollingInterval);
    });
  </script>
</body>
</html>
